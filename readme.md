# PDF timetable to dict parser

Convert C-Space timetable pdf export to the Json payload. 
```bash
git clone https://github.com/denver-code/pdftimetable-parser
cd pdftimetable-parser
poetry install 
poetry shell
python main.py
```